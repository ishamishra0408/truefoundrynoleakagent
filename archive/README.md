# archive/ — superseded variants (NOT the demo)

Kept for history only. The live system is `noleak/` (policy MCP proxy) + `scripts/` (TrueForge agent + demo).

- `spike/` — Friday-night spikes proving github-mcp-server reproduces the leak (pre-TrueForge).
- `agent/triage.mjs` + `mock-artifacts/` — v1 "artifact bucket" exfil variant with its own local policyCheck. Superseded by the comment-exfil channel and `noleak/policy.mjs`.
- `SCENARIO-realistic.md` — alternative support-API scenario write-up. Not built.
- `RUNBOOK-github-mcp-server.md` — runbook for the v1 variant.
- `preflight.mjs` — model-endpoint/tool-schema preflight for the v1 OpenAI-compat loop (pre-TrueForge).
